machine:
  node:
    version: 4.5.0 # Latest LTS Node.
  post:
      - npm install -g npm@3.8.9 # Latest LTS npm.

deployment:
  prod:
    branch: production
    commands:
      - 'bundle exec rake deploy:prod'
  dev:
    branch: master
    commands:
      - 'bundle exec rake deploy:dev'
