machine:
  node:
    version: 4.5.0 # Latest LTS Node.
  post:
      - npm install -g npm@3.8.9 # Latest LTS npm.

dependencies:
  pre:
      - sudo pip install awsebcli --upgrade --ignore-installed
deployment:
  production:
    branch: master
    commands:
      - 'bundle exec rake deploy:dev'
