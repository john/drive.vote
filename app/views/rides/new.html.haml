.to_or_from
  = @ride_zone.name

%h3.schedule
  = t(:schedule_ride)
.to_or_from
  = t(:to_or_from)
.to_or_from
  = t(:can_text_to) + ' ' + @ride_zone.phone_number.phony_formatted(normalize: :US, spaces: '-')

= render partial: 'form', locals: {rescheduling: false}
