:css
  h2 { font-size: 24px; font-weight: bold; color: #666666; margin: 30px 0 20px 20px; font-style: italic;}
  td { border: 0px solid grey;}
  .created_at {color: grey;}

= render partial: 'dispatch/nav'

%h2
  Edit Ride for #{@ride.name}

.col-md-12

  = render partial: 'rides/form', locals: {rescheduling: true, dispatcher: true}
