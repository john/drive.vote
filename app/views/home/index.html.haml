%h1
  Drive.Vote!

%p
  - if user_signed_in?
    #{t(:hi).humanize} #{current_user.name} [#{link_to 'sign out', destroy_user_session_path, method: :delete}]
  - else
    = link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path
