%p#notice= notice


%h1
  = @user.name
  %small
    = @user.user_type
  
%div= @user.description
%div= @user.email
%div= @user.phone_number.to_s.phony_formatted(normalize: :US, spaces: '-')

%br
%p
  People in your area looking for rides:
  %br
  - if @nearby_riders.present?
    %ul
      - @nearby_riders.each do |nbr|
        - if nbr.id != @user.id
          %li
            = nbr.name
  - else
    There aren't any.
    
%p
  There #{"is".pluralize(@nearby_drivers) } #{pluralize(@nearby_drivers, 'driver')} in your area.
  
%p
  #{link_to 'Share this', '#'} to let people know you're driving the vote!
  

-# %div{style: 'width: 400px;'}
-#   #map{style: 'width: 400px; height: 200px;'}
  
-# google maps api key: AIzaSyDefFnLJQKoz1OQGjaqaJPHMISVcnXZNPc
-# https://console.developers.google.com/apis/credentials/wizard?api=maps_backend&project=phonic-client-135123

-# gem: https://github.com/apneadiving/Google-Maps-for-Rails

-# <script src="//maps.google.com/maps/api/js?v=3.23&key=#{@google_api_key}"></script>
-# <script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
-# <script src='//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js' type='text/javascript'></script> <!-- only if you need custom infoboxes -->


-# :javascript
-#   handler = Gmaps.build('Google');
-#   handler.buildMap({ provider: { maxZoom: 15 }, internal: {id: 'map'}}, function(){
-#     markers = handler.addMarkers( #{raw @marker_hash.to_json} );
-#     handler.bounds.extendWith(markers);
-#     handler.fitMapToBounds();
-#   });